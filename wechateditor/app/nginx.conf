# 简化的nginx配置，用于微信公众号编辑器
server {
    listen 80;
    server_name localhost;
    
    location / {
        root /app/dist;
        try_files $uri $uri/ /index.html;
    }

    # 设置静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        root /app/dist;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}